<Window x:Class="FinanceFamilyApp.Views.BudgetWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Установка бюджета" 
        Height="500" 
        Width="450"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#2c3e50"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Height" Value="35"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Height" Value="35"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style TargetType="DatePicker">
            <Setter Property="Height" Value="35"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Категория расходов -->
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Категория расходов:"/>
        <ComboBox Grid.Row="0" Grid.Column="1" 
                  ItemsSource="{Binding ExpenseCategories}"
                  DisplayMemberPath="Name" 
                  SelectedItem="{Binding SelectedCategory}"/>

        <!-- Сумма бюджета -->
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Сумма бюджета:"/>
        <TextBox Grid.Row="1" Grid.Column="1" 
                 Text="{Binding Amount, StringFormat=N2}"/>

        <!-- Тип периода -->
        <TextBlock Grid.Row="2" Grid.Column="0" Text="Тип периода:"/>
        <ComboBox Grid.Row="2" Grid.Column="1" 
                  ItemsSource="{Binding PeriodTypes}"
                  SelectedItem="{Binding PeriodType}"/>

        <!-- Дата начала -->
        <TextBlock Grid.Row="3" Grid.Column="0" Text="Дата начала:"/>
        <DatePicker Grid.Row="3" Grid.Column="1" 
                    SelectedDate="{Binding StartDate}"/>

        <!-- Дата окончания -->
        <TextBlock Grid.Row="4" Grid.Column="0" Text="Дата окончания:"/>
        <DatePicker Grid.Row="4" Grid.Column="1" 
                    SelectedDate="{Binding EndDate}"/>

        <!-- Кнопки -->
        <StackPanel Grid.Row="8" Grid.Column="0" Grid.ColumnSpan="2"
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,20,0,0">
            <Button Content="Сохранить" 
                    Command="{Binding SaveCommand}"
                    Width="120" 
                    Height="35" 
                    Margin="0,0,10,0"
                    Background="#27ae60" 
                    Foreground="White"
                    FontWeight="SemiBold"/>

            <Button Content="Отмена" 
                    Command="{Binding CancelCommand}"
                    Width="120" 
                    Height="35"
                    Background="#e74c3c" 
                    Foreground="White"
                    FontWeight="SemiBold"/>
        </StackPanel>

        <!-- Пустое пространство для выравнивания -->
        <Border Grid.Row="7" Grid.Column="0" Grid.ColumnSpan="2"/>

    </Grid>
</Window>