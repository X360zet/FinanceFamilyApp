<Window x:Class="FinanceFamilyApp.Views.FamilyMemberWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:FinanceFamilyApp.Converters"
        Title="Добавление члена семьи"
        Height="650"
        Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#f5f5f5">

    <Window.Resources>
        <converters:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <converters:StringFormatConverter x:Key="StringFormatConverter"/>

        <Style TargetType="TextBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="TextAlignment" Value="Center"/>
        </Style>

        <Style TargetType="PasswordBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="5"/>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="Margin" Value="0,5,0,3"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="FontFamily" Value="Segoe UI Semibold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#4CAF50"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Width" Value="120"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="10,5"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#45a049"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#3d8b40"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button" x:Key="RedButtonStyle" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#f44336"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#e53935"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#d32f2f"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" 
                  HorizontalScrollBarVisibility="Disabled">
        <Grid Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Заголовок -->
            <TextBlock Grid.Row="0"
                       Text="{Binding IsEditMode, Converter={StaticResource StringFormatConverter}, ConverterParameter=Редактирование члена семьи;Добавление члена семьи}"
                       FontSize="22"
                       FontWeight="SemiBold"
                       Foreground="#333333"
                       Margin="0,0,0,15"
                       HorizontalAlignment="Center"
                       TextAlignment="Center"/>

            <!-- Форма -->
            <Border Grid.Row="1"
                    Background="White"
                    CornerRadius="10"
                    Padding="25,20"
                    Margin="0,0,0,15">
                <StackPanel>
                    <!-- Имя пользователя -->
                    <StackPanel Margin="0,0,0,8" 
                                Visibility="{Binding IsEditMode, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=False}">
                        <TextBlock Text="Имя пользователя:"/>
                        <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                                 HorizontalContentAlignment="Center"/>
                    </StackPanel>

                    <!-- Email -->
                    <StackPanel Margin="0,0,0,8"
                                Visibility="{Binding IsEditMode, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=False}">
                        <TextBlock Text="Email:"/>
                        <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"
                                 HorizontalContentAlignment="Center"/>
                    </StackPanel>

                    <!-- Пароль -->
                    <StackPanel Margin="0,0,0,8"
                                Visibility="{Binding IsEditMode, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=False}">
                        <TextBlock Text="Пароль:"/>
                        <PasswordBox x:Name="PasswordBox"/>
                    </StackPanel>

                    <!-- Подтверждение пароля -->
                    <StackPanel Margin="0,0,0,8"
                                Visibility="{Binding IsEditMode, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=False}">
                        <TextBlock Text="Подтверждение пароля:"/>
                        <PasswordBox x:Name="ConfirmPasswordBox"/>
                    </StackPanel>

                    <!-- Роль в семье -->
                    <StackPanel Margin="0,0,0,8">
                        <TextBlock Text="Роль в семье:"/>
                        <ComboBox ItemsSource="{Binding AvailableRoles}"
                                  SelectedItem="{Binding Role}"
                                  DisplayMemberPath="."
                                  SelectedIndex="1"
                                  HorizontalContentAlignment="Center"/>
                    </StackPanel>

                    <!-- Сообщение об ошибке -->
                    <Border Background="#FFF5F5" 
                            BorderBrush="#FFCDD2" 
                            BorderThickness="1" 
                            CornerRadius="4" 
                            Margin="0,10,0,5"
                            Padding="10,8"
                            Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}">
                        <TextBlock Text="{Binding ErrorMessage}"
                                   Foreground="#f44336"
                                   TextWrapping="Wrap"
                                   FontSize="12"
                                   HorizontalAlignment="Center"
                                   TextAlignment="Center"/>
                    </Border>

                    <!-- Примечание -->
                    <TextBlock Text="* - обязательные поля"
                               Foreground="#999999"
                               FontSize="11"
                               Margin="0,10,0,0"
                               HorizontalAlignment="Center"
                               Visibility="{Binding IsEditMode, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=False}"/>
                </StackPanel>
            </Border>

            <!-- Кнопки -->
            <StackPanel Grid.Row="2"
                        Orientation="Horizontal"
                        HorizontalAlignment="Center">
                <!-- Кнопка сохранения -->
                <Button Content="{Binding IsEditMode, Converter={StaticResource StringFormatConverter}, ConverterParameter=Сохранить;Добавить}"
                        Command="{Binding AddNewUserCommand}"/>

                <!-- Кнопка удаления (только в режиме редактирования) -->
                <Button Content="Удалить"
                        Command="{Binding DeleteFamilyMemberCommand}"
                        Style="{StaticResource RedButtonStyle}"
                        Visibility="{Binding IsEditMode, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                <!-- Кнопка отмены -->
                <Button Content="Отмена"
                        Command="{Binding CancelCommand}"
                        Style="{StaticResource RedButtonStyle}"/>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Window>