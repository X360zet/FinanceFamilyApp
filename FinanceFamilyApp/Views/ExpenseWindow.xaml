<Window x:Class="FinanceFamilyApp.Views.ExpenseWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Добавление расхода" 
        Height="550" 
        Width="450"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#2c3e50"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Height" Value="35"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Height" Value="35"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style TargetType="DatePicker">
            <Setter Property="Height" Value="35"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Категория -->
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Категория:"/>
        <ComboBox Grid.Row="0" Grid.Column="1" 
                  ItemsSource="{Binding ExpenseCategories}"
                  DisplayMemberPath="Name" 
                  SelectedItem="{Binding SelectedCategory}"/>

        <!-- Член семьи -->

        <!-- Сумма -->
        <TextBlock Grid.Row="2" Grid.Column="0" Text="Сумма:"/>
        <TextBox Grid.Row="2" Grid.Column="1" 
                 Text="{Binding Amount, StringFormat=N2}"/>

        <!-- Дата -->
        <TextBlock Grid.Row="3" Grid.Column="0" Text="Дата:"/>
        <DatePicker Grid.Row="3" Grid.Column="1" 
                    SelectedDate="{Binding Date}"/>

        <!-- Описание -->

        <!-- Кнопки -->
        <StackPanel Grid.Row="8" Grid.Column="0" Grid.ColumnSpan="2"
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,20,0,0">
            <Button Content="Сохранить" 
                    Command="{Binding SaveCommand}"
                    Width="120" 
                    Height="35" 
                    Margin="0,0,10,0"
                    Background="#27ae60" 
                    Foreground="White"
                    FontWeight="SemiBold"/>

            <Button Content="Отмена" 
                    Command="{Binding CancelCommand}"
                    Width="120" 
                    Height="35"
                    Background="#e74c3c" 
                    Foreground="White"
                    FontWeight="SemiBold"/>
        </StackPanel>

        <!-- Пустое пространство для выравнивания -->
        <Border Grid.Row="7" Grid.Column="0" Grid.ColumnSpan="2"/>

    </Grid>
</Window>